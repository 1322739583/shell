path="/disk1/绘本及其他/绘本/粉红猪小妹第2季53本绘本/2"
export path
cd ${path}
echo "pwd is $PWD"
result=`ls ${path}`
IFS=$'\n'
OLDIFS=$IFS
for i in $result
do
echo "i=${i} start"
filename=${i%.*}

echo "filename=${filename}";
echo "i=${1}"
qpdf -decrypt "$path/${i}" "$path/${filename}_decrypt.pdf"
done

